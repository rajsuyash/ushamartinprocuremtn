# BUILD-LOG

## M0 — Walking skeleton (2026-07-10)
Shipped: pnpm monorepo (Next 16 web + @pdi/shared), FastAPI engine (/health + DB ping, MISSING_ENV fail-fast), Docker Compose (postgres 16 + engine + web), web boot env validation via instrumentation.ts.
ACs passed: SKEL-AC1 (browser-verify, screenshot in .claude/verify-artifacts/), SKEL-AC2 (integration, exact JSON).
Surprises: (1) subagent tests passed only via exported shell env — import-time settings load fixed to lazy get_settings(); (2) engine pyproject lacked [build-system] so uv sync never installed the package in Docker — hatchling added; (3) host ports 8000/5432/5433 owned by other projects (aisewak, athena, sentinel) — engine→8100, postgres→5442 loopback; (4) docker-credential-desktop missing from non-login shell PATH — prefix /Applications/Docker.app/Contents/Resources/bin.

## M1 — Auth + data in (2026-07-10)
Shipped: E1-E17 schema + migrations (7 hostile constraint tests), deterministic FIX-1/2/3/5 seed (cover 18.2d/40d, rising band), Auth.js v5 credentials + JWT + middleware RBAC, envelope + withApiAuth guard (explicit roles required), streaming CSV/XLSX ingest lib (39 tests), upload stage/commit state machine, run lifecycle + engine stage stubs, /data page.
ACs passed (14/14 at tagged layers): F1-AC1/2/3, F1-ERR1/2/3, F2-AC1/2/3/4, F2-ERR1/2/3/4. Browser evidence: 7 screenshots in .claude/verify-artifacts/.
Surprises: (1) exceljs streaming reader broken on node25 — non-streaming fallback, bounded by 20MB cap; (2) papaparse abort() fires complete() first — reject-before-abort; (3) two opus reviews each surfaced real WARNs (bcrypt timing enumeration, blank-identity session, roles-by-omission, staged_rows blob on poll) — all fixed pre-commit; (4) runtime `pnpm start` in Docker = corepack network fetch at container start → exit 0; next invoked directly now; (5) Docker daemon wedged mid-build once — hard restart recovered.

## M2 — Demand forecasts (2026-07-10)
Shipped: engine data layer (committed reads + ISO-Monday weekly bucketing, active-month zero-fill), three demand models (seasonal-naive/ETS/deterministic LightGBM) with rolling-origin per-fold backtest + WAPE winner selection, LEAKAGE_GUARD + INSUFFICIENT_HISTORY handling, real POST /v1/forecast/demand with per-run idempotent E10 persistence + run warnings, /forecasts?tab=demand UI (chart, WAPE badge, insufficient-history chip, a11y table).
ACs passed (5/5 at tagged layers): F3-AC1 (integration: 6 series × 12 rows, live WAPE 3.4–4.4% ≤ 0.25), F3-AC2 (browser-verify: chart + badge, 0 console errors on route, screenshot m2-f3-ac2-demand-tab.png), F3-AC3 (unit determinism), F3-ERR1 (integration), F3-ERR2 (unit leakage guard). Suites: engine 47 pytest, web 80 vitest, typecheck+lint green.
R4 checkpoint: full run 18.2s on FIX-2 (<120s) — no queue needed.
Surprises: (1) engine Docker image lacked libgomp.so.1 (LightGBM OpenMP dep on python:3.12-slim) — crashloop on boot; libgomp1 added to Dockerfile; (2) favicon.ico 404 logs a console error on /login — app/icon.png added (effective next web image build), matters for "zero console errors" ACs in M5+.

## M3 — Price bands (2026-07-10)
Shipped: quantile LightGBM price bands (α .1/.5/.9) modeling horizon-ahead RETURNS (level-target collapsed quantile spread → coverage ~0.3-0.5; returns + heavy regularization fixed it), random-walk baseline + BASELINE_FALLBACK (<52w), monotonic repair + QUANTILE_REPAIRED counter, rolling-origin coverage/pinball backtest, real POST /v1/forecast/price with idempotent E11 persistence (orchestrator was already stage-generic — zero web changes for wiring), /forecasts?tab=price UI (stacked-area P10–P90 band + P50 line, coverage badge labeled "decision band", baseline-fallback chip, a11y table).
ACs passed (5/5 at tagged layers): F4-AC1 (integration: 2 grade_families × 3 horizons, coverage 0.769–0.808 ∈ [0.70,0.90], p10≤p50≤p90 all rows), F4-AC2 (browser-verify: shaded band + "decision band" badge, 0 console errors, screenshot m3-f4-ac2-price-tab.png), F4-AC3 (unit determinism), F4-ERR1 (fallback + chip), F4-ERR2 (crossing repair + counter). Suites: engine 67 pytest, web 91 + shared 41 vitest, typecheck+lint green.
Full run demand+price: 19.3s (<120s).
Surprises: (1) level-target quantile LightGBM on slow weekly series ≈ degenerate (lag_1 dominates) — returns-target is the fix; (2) app/icon.png didn't stop the browser's direct /favicon.ico request — real app/favicon.ico (ICO-wrapped PNG) did; (3) coverage badge pinned to 4w horizon (PRD doesn't specify which of 3 horizons — 4w matches F5's band4w rationale convention).

## M4 — Recommendation engine (2026-07-10)
Shipped: T19 MILP formulation spike (CP-SAT integer-kg scaling, assumption-literal relaxation, first-match play classifier, MC path model, JIT baseline — 6 ASSUMPTIONs logged); T20 input assembly (cover incl. open-PO arrival weeks, offers, policy loader, trailing-90d share denominator in one SQL, 3-horizon→weekly band interpolation); T21 solver + INDEPENDENT solution checker + seeded 500-path Monte Carlo + classifier + rationale builder; T22 real /v1/recommend (idempotent E13 persistence, PENDING→EXPIRED on new run, MISSING_PRICE_BAND per-series skip) + GET /api/recommendations (filterable, paginated).
ACs passed (6/6 at tagged layers): F5-AC1 (BUY_NOW + line + COVER_BELOW_FLOOR, deterministic), F5-AC2 (independent checker, hostile violations caught), F5-AC3 (WAIT, zero lines), F5-ERR1 (WC-relax→PARTIAL_BUY; NO_FEASIBLE_PLAN persisted ERROR row naming binding constraints), F5-ERR2 (MISSING_PRICE_BAND skip), F5-ERR3 (SOLVER_TIMEOUT). Suites: engine 107 pytest, web 94 + shared 41 vitest, typecheck+lint green. Full 3-stage run: 27s (<120s).
Live demoable check: 6/6 series → 2 BUY_NOW (breach series driver COVER_BELOW_FLOOR) + 4 WAIT (COVER_COMFORTABLE); expiry verified end-to-end.
Review (opus, FIX_FIRST → fixed): CRITICAL — MC costDelta 1000× too small (leftover kg-scale divide; sign-only test couldn't catch it → magnitude assertion added). WARNs: checker not fully solver-independent (now recomputes from inputs), defensive BUY_NOW could emit zero lines (restore line synthesized), λ_hold not strictly lexicographic (cost term ×HORIZON + dominance test), share-check false failures on lead-excluded suppliers (aligned).
Schema change (PRD Appendix B): E13 gains status ERROR + nullable play (CHECK play IS NULL ⟺ ERROR) so F5-ERR1 error rows persist. Two-phase migration — Postgres forbids using a fresh enum value in its adding transaction.
Surprises: (1) live tests depended on a leftover DONE run — pnpm seed alone broke them; session-scoped pipeline_run_id conftest fixture bootstraps demand+price once, suite now self-sufficient after seed; (2) BUY_NOW with empty drivers possible when cover between floor/target (solver buying against projected breach) — PROJECTED_COVER_BREACH/PRICE_OPPORTUNITY fallback drivers + lines⇒drivers invariant test; (3) FIX-3 seeded cover measures 16.6d not 18.2d headline (seed tuned against trailing consumption, spike A2 uses forward P50 denominator) — breach invariant (<21d) still holds; (4) CP-SAT status_name() broken in ortools 9.15 — numeric map; (5) TATA at 66.7% trailing share pre-existing — horizon buy dilutes below cap.

## M5 — Cockpit & decisions, P0 complete (2026-07-10)
Shipped: T23 dashboard (per-series tiles from STORED rationale.inputs, breach styling, band direction, best spread, run banner, alert seam), T24 recommendations list + detail (rationale panel / impact P10–P90 bar / order-lines / ERROR-row rendering, all from stored E13 JSON; override side-by-side), T25 decision API (SELECT FOR UPDATE decide-once + UNIQUE backstop, idempotency replay verifying same rec AND same action, NOTE_REQUIRED/OVERRIDE_PLAY_REQUIRED/DECISION_NOT_ALLOWED codes, append-only DecisionRecord), T26 decision bar UI (modals, disable-on-click, 10s timeout + same-key retry, inline errors, stale-tab 409 banner), T27 `pnpm regression` single-command P0 gate.
ACs passed: F6-AC1/AC2/AC3, F6-ERR1/ERR2 (browser-verify: 6 PASS walkthrough, screenshots m5-f6-*.png), F6-ERR3 (integration: idempotent retry tests). M5 gate: full P0 regression green in one suite — seed → typecheck → lint → 174 vitest (133 web + 41 shared) → 107 pytest.
Review (opus, APPROVE + WARNs fixed pre-commit): override missing from audit read model (blocked F6-AC3 side-by-side), uncapped page limit, unbounded note/idempotencyKey lengths, idempotency replay not verifying action match.
T23/T24/T25 ran as parallel subagents (approved g2 mod); one cross-agent seam: T24's chips test expected 6 distinct status classes, T25's PLAYS export satisfied T24's import — both resolved by main session.
Surprises: (1) favicon 404 fires once on first-ever load then never recurs (icon route warms after) — cosmetic; (2) FIX-3 cover displays 16.6d vs PRD's 18.2d headline (known from M4 — forward-P50 denominator; breach invariant holds, ACs don't assert 18.2 exactly).
