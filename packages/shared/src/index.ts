// ponytail: intentionally empty besides one export — packages/shared grows real
// modules (ingest, zod schemas, casing map) starting T8; this just proves the
// workspace wiring works end to end.
export const SHARED_PACKAGE_NAME = "@pdi/shared";
