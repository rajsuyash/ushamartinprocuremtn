---
description: Review the current diff against conventions, known pitfalls, and PRD constraints.
---

Delegate to the **code-reviewer** subagent on the current `git diff` (staged + unstaged).

Paste its findings verbatim. Fix all CRITICAL findings immediately; list WARNs with a one-line decision each (fix now / accept because…). Mandatory before completing any complexity:high task (F1, F2, F5, F6).
